import { menuContent } from "./objects/menuContent";
import { navRoutes } from "../../utils/content/objects/nav-routes";
import { authContent } from "./objects/authContent";
import { newBusiness } from "./objects/newBusiness";
export const content = [authContent, menuContent, navRoutes, newBusiness];

export const servUrl = `http://localhost:8081`;
